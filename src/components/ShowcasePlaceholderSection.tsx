import { panelClass } from "./ui";

export function ShowcasePlaceholderSection() {
  return (
    <section className="mt-6">
      <div className={panelClass}>
        <h2 className="text-xl font-semibold text-white">Showcase</h2>
        <p className="mt-2 text-sm text-slate-300">This tab is intentionally empty for now. Content will be added later.</p>
      </div>
    </section>
  );
}
